# Generated by openapi-typescript (npm run api:generate). The schema CI job
# regenerates this file and diff-checks it, so it must match the generator's
# own formatting — prettier must not touch it.
src/types/api-schema.ts
dist/
